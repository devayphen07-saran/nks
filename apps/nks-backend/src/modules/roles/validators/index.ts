/**
 * Roles Module Validators
 *
 * Custom validators for roles (beyond Zod schema validation):
 * - System role code validation
 * - Role code uniqueness per store
 * - Store ownership verification
 * - Role assignment validations
 */

// Validators will be added here as needed
// Currently, Zod schemas in DTOs handle primary validation
